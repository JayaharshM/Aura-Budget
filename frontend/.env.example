# Copy this file to .env.local for local development, or set these variables
# in your hosting platform's environment settings for production.

# The base URL of your backend API server (no trailing slash).
# Example (local):      http://localhost:5001
# Example (production): https://your-backend.onrender.com
VITE_API_URL=
