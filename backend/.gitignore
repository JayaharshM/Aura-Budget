# Dependencies
node_modules/

# Environment & secrets — NEVER commit these
.env
atlas-credentials.env
*.env

# Keep the template
!.env.example

# Uploaded files (runtime directory, not source)
uploads/*
!uploads/.gitkeep

# macOS
.DS_Store

# Python cache
__pycache__/
*.pyc
